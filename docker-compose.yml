services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    command: sh -c "uvicorn app.main:app --host 0.0.0.0 --port $${PORT:-8000} --reload"
